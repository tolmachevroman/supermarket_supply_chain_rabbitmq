defmodule SupermarketSupplyChainRabbitmqTest do
  use ExUnit.Case
  doctest SupermarketSupplyChainRabbitmq

  test "greets the world" do
    assert SupermarketSupplyChainRabbitmq.hello() == :world
  end
end
